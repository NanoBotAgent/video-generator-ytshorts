# PyTorch CPU-only wheels (must use --extra-index-url as a global option + explicit +cpu
# version tags, otherwise pip silently resolves the default CUDA build from PyPI, which
# pulls in ~1.7GB of unused nvidia-*-cu12 packages on a CPU-only runner)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.0+cpu
torchvision==0.19.0+cpu
torchaudio==2.4.0+cpu

# Core ML libraries - transformers 4.49+ for Moonshine support
# huggingface-hub must be >=0.26.0 for transformers==4.49.0 (was pinned to 0.24.0, which
# caused a hard ResolutionImpossible failure in CI)
transformers==4.49.0
accelerate==0.33.0
safetensors==0.4.4
huggingface-hub==0.26.2

# Audio processing
librosa==0.10.2
soundfile==0.12.1
scipy==1.13.0
numpy==1.26.0

# TTS: Fish Audio S2.1 Pro via the free hosted API (scripts/01_tts.py), with a local
# espeak-ng CLI fallback (installed as a system package in the workflow) if the API key
# is missing or the request fails. Intentionally unpinned - this SDK tracks a live,
# fast-moving hosted API rather than a fixed model artifact, so an old pin could miss
# the current free-tier model name/params. pyttsx3 was tried before espeak-ng but its
# ctypes driver silently no-ops in headless CI - see scripts/01_tts.py history. See
# README for why Step-Audio-EditX / local Fish Audio S2 Pro via s2.cpp aren't used.
fish-audio-sdk

# Utilities
tqdm==4.66.0
pyyaml==6.0.1
requests==2.32.3
